name: Bot Cíclico - Execução a cada 6h

on:
  schedule:
    # Executa a cada 6 horas (00:00, 06:00, 12:00, 18:00 UTC)
    - cron: '0 */6 * * *'
  
  workflow_dispatch:
    # Permite execução manual
    inputs:
      duracao_horas:
        description: 'Duração da execução em horas'
        required: false
        default: '5'
        type: string

env:
  REPO_OWNER: Alisson990jd
  REPO_NAME: apiss
  BOT_ZIP: bot.zip
  BOT_BRANCH: main  # ou master, dependendo da sua branch principal

jobs:
  executar-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 horas de timeout máximo
    
    steps:
      - name: 📋 Configurar Git
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
      
      - name: 🔧 Instalar Dependências do Sistema
        run: |
          echo "📄 Atualizando repositórios..."
          sudo apt-get update -qq
          
          echo "📦 Instalando FFmpeg..."
          sudo apt-get install -y -qq ffmpeg
          
          echo "📦 Instalando Python 3 e pip..."
          sudo apt-get install -y -qq python3 python3-pip python3-venv
          
          echo "📦 Instalando unzip..."
          sudo apt-get install -y -qq unzip curl wget
          
          echo "✅ Dependências do sistema instaladas com sucesso!"
      
      - name: 🔧 Instalar yt-dlp
        run: |
          echo "📦 Instalando yt-dlp..."
          sudo wget https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -O /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp
          yt-dlp --version
          echo "✅ yt-dlp instalado com sucesso!"
      
      - name: 🔧 Instalar rclone
        run: |
          echo "📦 Instalando rclone..."
          curl https://rclone.org/install.sh | sudo bash
          rclone version
          echo "✅ rclone instalado com sucesso!"
      
      - name: 📥 Baixar bot.zip do repositório
        env:
          GH_TOKEN: ${{ secrets.GHTOKEN }}
        run: |
          echo "📥 Baixando bot.zip do repositório..."
          
          # Usar as variáveis de ambiente do repositório
          OWNER="$REPO_OWNER"
          REPO="$REPO_NAME"
          FILE="bot.zip"
          
          echo "📦 Repositório: $OWNER/$REPO"
          echo "📄 Arquivo: $FILE"
          
          # Método 1: Tentar download direto via raw.githubusercontent.com
          echo ""
          echo "🔄 Método 1: Download direto (raw.githubusercontent.com)..."
          
          # Tentar branch main
          RAW_URL="https://raw.githubusercontent.com/$OWNER/$REPO/main/$FILE"
          echo "🔗 Tentando: $RAW_URL"
          
          HTTP_CODE=$(curl -L -w "%{http_code}" -s \
            -H "Authorization: token $GH_TOKEN" \
            "$RAW_URL" \
            -o bot.zip)
          
          echo "📡 HTTP Status: $HTTP_CODE"
          
          # Se falhar, tentar branch master
          if [ "$HTTP_CODE" != "200" ]; then
            echo "⚠️ Falhou na branch main, tentando master..."
            RAW_URL="https://raw.githubusercontent.com/$OWNER/$REPO/master/$FILE"
            echo "🔗 Tentando: $RAW_URL"
            
            HTTP_CODE=$(curl -L -w "%{http_code}" -s \
              -H "Authorization: token $GH_TOKEN" \
              "$RAW_URL" \
              -o bot.zip)
            
            echo "📡 HTTP Status: $HTTP_CODE"
          fi
          
          # Verificar se o arquivo foi baixado corretamente
          if [ -f bot.zip ]; then
            BOT_SIZE=$(stat -c%s bot.zip 2>/dev/null || stat -f%z bot.zip 2>/dev/null)
            echo "📊 Tamanho: $BOT_SIZE bytes"
            
            # Verificar se é um ZIP válido
            if file bot.zip | grep -q "Zip archive"; then
              echo "✅ Método 1 bem-sucedido!"
            else
              echo "⚠️ Método 1 baixou arquivo inválido, tentando método 2..."
              rm -f bot.zip
              HTTP_CODE="000"
            fi
          fi
          
          # Método 2: Download via GitHub API
          if [ "$HTTP_CODE" != "200" ]; then
            echo ""
            echo "🔄 Método 2: Download via API do GitHub..."
            
            # Tentar branch main
            API_URL="https://api.github.com/repos/$OWNER/$REPO/contents/$FILE"
            echo "🔗 Tentando: $API_URL"
            
            RESPONSE=$(curl -s \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "$API_URL")
            
            # Verificar se obteve resposta válida
            if echo "$RESPONSE" | python3 -c "import sys, json; json.load(sys.stdin)" 2>/dev/null; then
              echo "📦 Decodificando conteúdo base64..."
              
              # Criar script temporário para evitar erro de sintaxe YAML
              cat > decode_script.py << 'EOF'
          import sys, json, base64
          try:
              data = json.load(sys.stdin)
              if 'content' in data:
                  content = base64.b64decode(data['content'])
                  with open('bot.zip', 'wb') as f:
                      f.write(content)
                  print('✅ Arquivo decodificado com sucesso!')
              else:
                  print('❌ Resposta da API não contém campo content')
                  print('Resposta parcial:', json.dumps(data)[:200])
                  sys.exit(1)
          except Exception as e:
              print(f'❌ Erro ao processar resposta: {e}')
              sys.exit(1)
          EOF
              
              # Executar o script criado
              echo "$RESPONSE" | python3 decode_script.py
              
              if [ $? -eq 0 ] && [ -f bot.zip ]; then
                BOT_SIZE=$(stat -c%s bot.zip 2>/dev/null || stat -f%z bot.zip 2>/dev/null)
                echo "📊 Tamanho: $BOT_SIZE bytes"
                
                if file bot.zip | grep -q "Zip archive"; then
                  echo "✅ Método 2 bem-sucedido!"
                else
                  echo "❌ Método 2 baixou arquivo inválido"
                fi
              fi
              rm decode_script.py
            else
              echo "❌ Resposta da API inválida"
              echo "Resposta: ${RESPONSE:0:500}"
            fi
          fi
          
          # Verificação final
          echo ""
          echo "🔍 Verificação final..."
          
          if [ ! -f bot.zip ]; then
            echo "❌ ERRO: bot.zip não foi baixado!"
            echo ""
            echo "💡 Possíveis soluções:"
            echo "   1. Verifique se o arquivo bot.zip existe no repositório $OWNER/$REPO"
            echo "   2. Verifique se o token GHTOKEN tem permissões de leitura"
            echo "   3. Verifique se o arquivo está na branch main ou master"
            echo ""
            echo "🔗 Verifique manualmente em:"
            echo "   https://github.com/$OWNER/$REPO/blob/main/bot.zip"
            exit 1
          fi
          
          BOT_SIZE=$(stat -c%s bot.zip 2>/dev/null || stat -f%z bot.zip 2>/dev/null)
          echo "📊 Tamanho final: $BOT_SIZE bytes ($(echo "scale=2; $BOT_SIZE / 1024 / 1024" | bc 2>/dev/null || echo "?") MB)"
          
          # Verificar tipo do arquivo
          echo "🔍 Tipo do arquivo:"
          file bot.zip
          
          # Verificar se é HTML (erro 404)
          if file bot.zip | grep -q "HTML"; then
            echo "❌ ERRO: O arquivo baixado é HTML (provavelmente página de erro 404)"
            echo "📄 Primeiros 200 caracteres:"
            head -c 200 bot.zip
            exit 1
          fi
          
          # Verificar se é realmente um ZIP
          if ! file bot.zip | grep -q "Zip archive"; then
            echo "❌ ERRO: O arquivo não é um ZIP válido!"
            echo "📄 Primeiros bytes (hex):"
            hexdump -C bot.zip | head -5
            exit 1
          fi
          
          # Testar integridade do ZIP
          echo "🧪 Testando integridade..."
          if unzip -t bot.zip >/dev/null 2>&1; then
            echo "✅ bot.zip é válido e íntegro!"
          else
            echo "❌ ERRO: bot.zip está corrompido!"
            unzip -t bot.zip
            exit 1
          fi
          
          echo ""
          echo "✅ bot.zip baixado e verificado com sucesso!"
      
      - name: 📂 Extrair bot.zip
        run: |
          echo "📂 Extraindo bot.zip..."
          
          # Verificar novamente se bot.zip existe
          if [ ! -f bot.zip ]; then
            echo "❌ Erro: bot.zip não encontrado!"
            ls -la
            exit 1
          fi
          
          # Verificar tipo antes de extrair
          echo "🔍 Verificando tipo do arquivo:"
          file bot.zip
          
          # Criar diretório de trabalho
          mkdir -p bot_workspace
          
          # Extrair bot.zip com verbose
          echo "📦 Extraindo arquivos..."
          if unzip -q bot.zip -d bot_workspace; then
            echo "✅ Extração bem-sucedida!"
          else
            echo "❌ Erro ao extrair bot.zip!"
            echo "📊 Informações do arquivo:"
            ls -lh bot.zip
            file bot.zip
            echo "📄 Tentando listar conteúdo do ZIP:"
            unzip -l bot.zip || true
            exit 1
          fi
          
          # Verificar se a extração foi bem-sucedida
          if [ ! -d bot_workspace ]; then
            echo "❌ Erro: Diretório bot_workspace não foi criado!"
            exit 1
          fi
          
          # Verificar se há arquivos no diretório
          FILE_COUNT=$(find bot_workspace -type f | wc -l)
          echo "📊 Arquivos extraídos: $FILE_COUNT"
          
          if [ "$FILE_COUNT" -eq 0 ]; then
            echo "⚠️ ATENÇÃO: Nenhum arquivo foi extraído!"
            exit 1
          fi
          
          # Listar arquivos extraídos
          echo "📋 Arquivos extraídos:"
          ls -lah bot_workspace/
          
          echo "✅ bot.zip extraído com sucesso!"
      
      - name: 🐍 Instalar Dependências Python
        run: |
          echo "🐍 Instalando dependências Python..."
          
          cd bot_workspace
          
          # Atualizar pip
          python3 -m pip install --upgrade pip
          
          # Instalar todas as dependências
          echo "📦 Instalando pacotes Python..."
          python3 -m pip install --upgrade \
            discord.py \
            aiohttp \
            python-dateutil \
            kickapi \
            requests \
            packaging \
            google-auth-oauthlib \
            google-auth \
            google-api-python-client \
            google-cloud-resource-manager \
            google-cloud-service-usage \
            google-cloud-api-keys \
            google-generativeai \
            protobuf \
            cachetools \
            pyasn1 \
            pyasn1-modules \
            rsa \
            streamlink
          
          echo "✅ Dependências Python instaladas com sucesso!"
      
      - name: 🔍 Verificar Arquivos Necessários
        run: |
          echo "🔍 Verificando arquivos necessários..."
          
          cd bot_workspace
          
          # Verificar se start.py existe
          if [ ! -f start.py ]; then
            echo "❌ Erro: start.py não encontrado!"
            exit 1
          fi
          
          # Verificar se dc.py existe
          if [ ! -f dc.py ]; then
            echo "❌ Erro: dc.py não encontrado!"
            exit 1
          fi
          
          # Listar arquivos .json
          echo "📄 Arquivos JSON encontrados:"
          ls -lah *.json 2>/dev/null || echo "Nenhum arquivo JSON encontrado"
          
          echo "✅ Arquivos verificados com sucesso!"
      
      - name: 🚀 Executar start.py
        id: executar_bot
        env:
          DURACAO_HORAS: ${{ github.event.inputs.duracao_horas || '5' }}
        run: |
          echo "🚀 Iniciando start.py..."
          echo "⏱️ Duração configurada: $DURACAO_HORAS horas"
          
          cd bot_workspace
          
          # Calcular tempo em segundos (5 horas = 18000 segundos)
          DURACAO_SEGUNDOS=$((DURACAO_HORAS * 3600))
          echo "⏱️ Duração em segundos: $DURACAO_SEGUNDOS"
          
          # Criar script de execução com timeout
          cat > run_with_timeout.sh << 'EOF'
          #!/bin/bash

          DURACAO=$1
          LOG_FILE="bot_execution.log"

          echo "🎬 Iniciando execução do bot..."
          echo "⏱️ Tempo máximo: $DURACAO segundos"
          echo "📝 Log será salvo em: $LOG_FILE"

          # Executar start.py em background e capturar PID
          python3 -u start.py > "$LOG_FILE" 2>&1 &
          BOT_PID=$!

          echo "✅ Bot iniciado com PID: $BOT_PID"
          echo "⏳ Aguardando $DURACAO segundos..."

          # Aguardar pela duração especificada
          sleep $DURACAO

          # Verificar se o processo ainda está rodando
          if kill -0 $BOT_PID 2>/dev/null; then
            echo "ℹ️ Tempo esgotado. Parando o bot..."
            
            # Enviar SIGTERM (interrupção graceful)
            kill -TERM $BOT_PID 2>/dev/null
            
            # Aguardar até 30 segundos para o processo terminar
            for i in {1..30}; do
              if ! kill -0 $BOT_PID 2>/dev/null; then
                echo "✅ Bot parado gracefully"
                exit 0
              fi
              sleep 1
            done
            
            # Se ainda estiver rodando, forçar
            if kill -0 $BOT_PID 2>/dev/null; then
              echo "⚠️ Processo não respondeu, forçando encerramento..."
              kill -KILL $BOT_PID 2>/dev/null
            fi
            
            echo "✅ Bot parado (forcefully)"
          else
            echo "ℹ️ Bot já finalizou antes do timeout"
          fi

          # Exibir últimas linhas do log
          echo ""
          echo "📋 Últimas 50 linhas do log:"
          echo "─────────────────────────────────────────"
          tail -n 50 "$LOG_FILE"
          echo "─────────────────────────────────────────"

          exit 0
          EOF
          
          chmod +x run_with_timeout.sh
          
          # Executar o script
          ./run_with_timeout.sh $DURACAO_SEGUNDOS
          
          echo "✅ Execução do bot concluída!"
      
      - name: 📊 Exibir Estatísticas de Execução
        if: always()
        run: |
          echo "📊 Estatísticas de Execução"
          echo "─────────────────────────────────────────"
          
          cd bot_workspace
          
          # Verificar se data.json existe e mostrar estatísticas
          if [ -f data.json ]; then
            echo "📄 data.json encontrado!"
            
            # Contar streamers
            STREAMERS=$(python3 -c "import json; data=json.load(open('data.json')); print(len(data.get('streamers', {})))" 2>/dev/null || echo "0")
            echo "👥 Streamers monitorados: $STREAMERS"
            
            # Contar VODs processados
            VODS=$(python3 -c "import json; data=json.load(open('data.json')); print(len(data.get('processed_vods', {})))" 2>/dev/null || echo "0")
            echo "🎬 VODs processados: $VODS"
          else
            echo "⚠️ data.json não encontrado"
          fi
          
          # Mostrar espaço em disco usado
          echo ""
          echo "💾 Espaço em disco usado:"
          du -sh . 2>/dev/null || echo "Não foi possível calcular"
          
          # Listar arquivos grandes
          echo ""
          echo "📦 Arquivos maiores que 10MB:"
          find . -type f -size +10M -exec ls -lh {} \; 2>/dev/null || echo "Nenhum arquivo grande encontrado"
          
          echo "─────────────────────────────────────────"
      
      - name: 🗜️ Compactar bot_workspace
        if: always()
        run: |
          echo "🗜️ Compactando bot_workspace..."
          
          # Remover bot.zip antigo
          rm -f bot.zip
          
          # Compactar todo o diretório bot_workspace
          cd bot_workspace
          zip -r -q ../bot.zip .
          cd ..
          
          # Verificar tamanho do novo bot.zip
          NEW_SIZE=$(stat -f%z bot.zip 2>/dev/null || stat -c%s bot.zip 2>/dev/null)
          echo "📊 Tamanho do novo bot.zip: $NEW_SIZE bytes"
          
          # Converter para MB
          SIZE_MB=$(echo "scale=2; $NEW_SIZE / 1024 / 1024" | bc)
          echo "📊 Tamanho em MB: ${SIZE_MB}MB"
          
          echo "✅ bot_workspace compactado com sucesso!"
      
      - name: 📤 Fazer Upload do novo bot.zip para o repositório
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GHTOKEN }}
        run: |
          echo "📤 Fazendo upload do novo bot.zip..."
          
          # Obter SHA do arquivo atual
          echo "🔍 Obtendo SHA do arquivo atual..."
          SHA_RESPONSE=$(curl -s -H "Authorization: Bearer $GH_TOKEN" \
            "https://api.github.com/repos/$REPO_OWNER/$REPO_NAME/contents/$BOT_ZIP")
          
          SHA=$(echo "$SHA_RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin).get('sha', ''))")
          
          if [ -z "$SHA" ]; then
            echo "⚠️ Arquivo não existe, será criado um novo."
          else
            echo "✅ SHA obtido: ${SHA:0:7}..."
          fi
          
          # --- CORREÇÃO AQUI ---
          # Criar script Python para gerar o JSON do payload
          # Isso evita o erro "Argument list too long" do shell
          
          cat > create_payload.py << 'EOF'
          import sys, json, base64, os
          
          file_path = 'bot.zip'
          sha = sys.argv[1] if len(sys.argv) > 1 else ""
          message = "🤖 Bot atualizado automaticamente - " + os.environ.get('DATE_MSG', 'update')
          
          try:
              # Ler o arquivo ZIP e codificar em Base64
              with open(file_path, "rb") as f:
                  encoded = base64.b64encode(f.read()).decode('utf-8')
              
              # Criar o objeto JSON
              payload = {
                  "message": message,
                  "content": encoded
              }
              
              # Adicionar SHA apenas se ele existir (para update)
              if sha and sha != "None":
                  payload["sha"] = sha
              
              # Salvar em arquivo
              with open("payload.json", "w") as outfile:
                  json.dump(payload, outfile)
              
              print("✅ Payload JSON criado com sucesso!")
              
          except Exception as e:
              print(f"❌ Erro ao criar payload: {e}")
              sys.exit(1)
          EOF
          
          # Executar o script Python
          export DATE_MSG="$(date -u +%Y-%m-%d\ %H:%M:%S\ UTC)"
          python3 create_payload.py "$SHA"
          
          # Fazer upload usando curl lendo o arquivo (-d @payload.json)
          echo "📡 Enviando para API do GitHub..."
          
          HTTP_CODE=$(curl -s -o response.json -w "%{http_code}" \
            -X PUT \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$REPO_OWNER/$REPO_NAME/contents/$BOT_ZIP" \
            -d @payload.json)
            
          echo "📡 HTTP Status: $HTTP_CODE"
          
          if [ "$HTTP_CODE" == "200" ] || [ "$HTTP_CODE" == "201" ]; then
            echo "✅ Upload concluído com sucesso!"
          else
            echo "❌ Erro no upload!"
            echo "📄 Resposta da API:"
            cat response.json
            exit 1
          fi
          
          # Limpar arquivos temporários
          rm payload.json create_payload.py response.json
      
      - name: 📋 Resumo da Execução
        if: always()
        run: |
          echo ""
          echo "╔════════════════════════════════════════╗"
          echo "📋 RESUMO DA EXECUÇÃO"
          echo "╚════════════════════════════════════════╝"
          echo "📅 Data/Hora: $(date -u +%Y-%m-%d\ %H:%M:%S\ UTC)"
          echo "⏱️ Duração configurada: ${{ github.event.inputs.duracao_horas || '5' }} horas"
          echo "🔄 Próxima execução: Em ~6 horas"
          echo "╔════════════════════════════════════════╗"
          echo "✅ Workflow concluído com sucesso!"
          echo "╚════════════════════════════════════════╝"
      
      - name: 🧹 Limpeza de Arquivos Temporários
        if: always()
        run: |
          echo "🧹 Limpando arquivos temporários..."
          
          # Remover diretório de trabalho
          rm -rf bot_workspace
          
          # Remover bot.zip local
          rm -f bot.zip
          
          echo "✅ Limpeza concluída!"
